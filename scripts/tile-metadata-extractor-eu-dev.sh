#!/usr/bin/env bash
# Rescale automation tile command — Metadata Extractor [DEV] (EU)
# Paste ONLY this into the tile Command field.
# Job command must have copied run_metadata.sh to /enc/tmp first (see run_job_prep.sh).

bash /enc/tmp/run_metadata.sh
