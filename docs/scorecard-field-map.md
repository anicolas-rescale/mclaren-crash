# McLaren ES-2re pole scorecard → VTP train targets
#
# Packaged copy also lives in automation-ai-extractor:
#   src/core/solvers/ls_dyna/scorecard_field_map.json
#   src/core/solvers/ls_dyna/probe_map.json

See those JSON files for the frozen probe node/load-cell IDs (from HFuKPb
binout legends) and metric→field mapping.

Hard rule: surrogate predicted VTP alone must support the full ~40 metrics.
